import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.react.starter",
  appName: "task-manager-react",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
